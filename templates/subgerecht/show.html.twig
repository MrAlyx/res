{% extends 'base.html.twig' %}

{% block title %}Subgerecht{% endblock %}

{% block body %}
    <h1>Subgerecht</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subgerecht.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ subgerecht.naam }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subgerecht_index') }}">back to list</a>

    <a href="{{ path('subgerecht_edit', {'id': subgerecht.id}) }}">edit</a>

    {{ include('subgerecht/_delete_form.html.twig') }}
{% endblock %}
